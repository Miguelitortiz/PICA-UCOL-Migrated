---
import LoginPanel from "../components/LoginPanel.astro";

// Si el usuario ya está autenticado, redirigir al dashboard
if (Astro.cookies.has('pica_session')) {
  return Astro.redirect('/');
}
---

<LoginPanel />
